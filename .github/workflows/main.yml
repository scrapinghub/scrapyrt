name: CI
# TODO: Get rid of warnings when running tox with the latest version of Python.
# TODO: Support offline coverage gathering (see flake8-scrapy)
on:
  push:
    branches:
    - master
  pull_request:
jobs:
  tox:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
        - python-version: "3.13"
          tox: "pre-commit"
        - python-version: "3.9"
          tox: "mypy"
        - python-version: "3.9"
          tox: "min"
        - python-version: "3.10"
        - python-version: "3.11"
        - python-version: "3.12"
        - python-version: "3.13"
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: Run tox
      run: |
        pip install -U tox
        tox -e ${{ matrix.tox || 'py' }}
